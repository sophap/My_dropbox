<div style="display: flex; justify-content: center; align-items: center;">
<%= form_with(model: dock) do |form| %>
  <% if dock.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(dock.errors.count, "error") %> prohibited this dock from being saved:</h2>

      <ul>
        <% dock.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <% if current_user %>

  <div>
    <%= form.hidden_field :user_id, value: current_user.id %>
  </div>
  

  <div class="d-flex justify-content-center align-items-center">
    <%= form.file_field :files, multiple: true, name: 'dock[files][]', class: 'form-control' %>
  </div>

</br>
</br>
  <div style="text-align: center; padding-top: 20px;">
    <%= form.submit('Upload', class: 'btn btn-outline-info') %>
  </div>

  <% end %>
<% end %>
</div>
